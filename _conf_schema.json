{
  "download_dir": {
    "type": "string",
    "description": "漫画下载目录",
    "hint": "下载的漫画将保存到此目录，可使用相对路径或绝对路径",
    "default": "./downloads"
  },
  "image_suffix": {
    "type": "string",
    "description": "图片格式",
    "hint": "下载图片的格式转换",
    "default": ".jpg",
    "options": [
      ".jpg",
      ".png",
      ".webp"
    ]
  },
  "client_type": {
    "type": "string",
    "description": "客户端类型",
    "hint": "api模式使用移动端API兼容性好，html模式使用网页端效率高但限制IP地区",
    "default": "api",
    "options": [
      "api",
      "html"
    ]
  },
  "client_domain": {
    "type": "string",
    "description": "自定义域名列表",
    "hint": "逗号分隔的 JM 域名，留空则由 jmcomic 自动选择；当默认域名被墙时可手动指定，如 18comic.vip,18comic.org",
    "default": ""
  },
  "retry_times": {
    "type": "int",
    "description": "请求重试次数",
    "hint": "网络请求失败时的重试次数，0 表示使用 jmcomic 默认值(5)",
    "default": 0
  },
  "use_proxy": {
    "type": "bool",
    "description": "是否使用代理",
    "hint": "某些地区可能需要代理才能访问JM网站",
    "default": false
  },
  "proxy_url": {
    "type": "string",
    "description": "代理服务器地址",
    "hint": "格式: http://host:port 或 socks5://host:port",
    "default": ""
  },
  "max_concurrent_photos": {
    "type": "int",
    "description": "最大并发章节数",
    "hint": "同时下载的章节数量，建议3-5",
    "default": 3
  },
  "max_concurrent_images": {
    "type": "int",
    "description": "最大并发图片数",
    "hint": "每个章节同时下载的图片数量，建议5-10",
    "default": 5
  },
  "pack_format": {
    "type": "string",
    "description": "打包格式",
    "hint": "下载完成后的打包格式。long_img 为纵向拼接长图(过长自动分段并打包为zip)；none 为不打包、仅本地保存不发送",
    "default": "zip",
    "options": [
      "zip",
      "pdf",
      "long_img",
      "none"
    ]
  },
  "pack_password": {
    "type": "string",
    "description": "打包密码",
    "hint": "为ZIP或PDF设置密码，留空则不加密",
    "default": ""
  },
  "filename_show_password": {
    "type": "bool",
    "description": "文件名显示密码",
    "hint": "开启后在文件名末尾添加 #PWxxx 提示（仅当设置了密码时生效）。注意：会把打包密码暴露在文件名/聊天记录/日志中，请谨慎开启",
    "default": false
  },
  "auto_delete_after_send": {
    "type": "bool",
    "description": "发送后自动删除",
    "hint": "发送文件后是否自动删除本地文件以节省空间",
    "default": true
  },
  "send_cover_preview": {
    "type": "bool",
    "description": "发送封面预览",
    "hint": "下载前是否发送漫画封面图片预览",
    "default": true
  },
  "show_download_progress": {
    "type": "bool",
    "description": "发送下载进度",
    "hint": "下载本子时按 25% 步进推送进度消息，关闭可减少消息刷屏",
    "default": true
  },
  "cover_recall_enabled": {
    "type": "bool",
    "description": "封面消息自动撤回",
    "hint": "开启后封面预览消息会自动撤回（仅支持 QQ/NapCat 平台）",
    "default": false
  },
  "auto_recall_enabled": {
    "type": "bool",
    "description": "文件消息自动撤回",
    "hint": "发送文件后自动撤回（仅支持 QQ/NapCat 平台）",
    "default": false
  },
  "auto_recall_delay": {
    "type": "int",
    "description": "撤回延迟（秒）",
    "hint": "发送后多少秒自动撤回，建议30-120",
    "default": 60
  },
  "enabled_groups": {
    "type": "string",
    "description": "启用的群列表",
    "hint": "以逗号分隔的群号列表，留空则所有群都启用",
    "default": ""
  },
  "admin_only": {
    "type": "bool",
    "description": "仅管理员可用",
    "hint": "开启后只有管理员列表中的用户才能使用插件",
    "default": false
  },
  "admin_list": {
    "type": "string",
    "description": "管理员用户ID列表",
    "hint": "以逗号分隔的用户ID列表，这些用户拥有管理员权限",
    "default": ""
  },
  "jm_username": {
    "type": "string",
    "description": "JM账号用户名",
    "hint": "填写后可自动登录，解锁收藏夹等功能（可选）",
    "default": ""
  },
  "jm_password": {
    "type": "string",
    "description": "JM账号密码",
    "hint": "配合用户名使用，支持自动登录（可选）",
    "default": ""
  },
  "search_page_size": {
    "type": "int",
    "description": "搜索结果每页数量",
    "hint": "每次搜索返回的结果数量",
    "default": 5
  },
  "daily_download_limit": {
    "type": "int",
    "description": "每日下载次数限制",
    "hint": "每个用户每天最多下载次数，0 表示不限制。注意：配额数据库异常时会降级放行(fail-open)",
    "default": 0
  },
  "subscribe_check_interval": {
    "type": "int",
    "description": "订阅检查间隔（秒）",
    "hint": "后台检查订阅本子更新的间隔秒数，建议>=1800，0 表示关闭后台检查(从0改为非0需重载插件生效)",
    "default": 3600
  },
  "debug_mode": {
    "type": "bool",
    "description": "调试模式",
    "hint": "开启后会输出详细的调试日志",
    "default": false
  },
  "qq_file_size_limit_mb": {
    "type": "int",
    "description": "QQ官方平台上传文件大小上限(MB)",
    "hint": "QQ官方平台(stgw网关)对单次上传有body大小限制(通常约5MB)。当打包文件超过此值时，自动拆分为多卷独立文件分批发送(如 xxx_part1.zip, xxx_part2.zip)。设为0则不拆分。非QQ官方平台忽略此配置",
    "default": 5
  }
}