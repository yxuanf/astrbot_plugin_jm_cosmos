# 更新日志

所有版本更新记录。

## **v2.7.7** (2026-06-23)

### 新增功能
- **新增跨平台独立分卷打包与顺序发送**
  - ZIP、PDF 及长图生成的 ZIP 支持按文件大小阈值拆分为 `文件名_part1`、`文件名_part2` 等多个独立文件
  - 每一卷均可独立打开或解压；启用加密时，各卷统一使用当前配置的密码
  - 第一卷携带完整下载结果，后续卷显示 `[序号/总数]`，所有卷发送完成后再统一执行自动清理
  - 单张图片超过分卷阈值时不丢弃，改为单独成卷并记录警告

- **新增 Telegram 文件上传配置**
  - `telegram_upload_timeout`：媒体上传超时，默认 `180` 秒
  - `telegram_file_size_limit_mb`：单卷大小阈值，默认 `5` MB；设为 `0` 可禁用 Telegram 分卷
  - Telegram 不限制分卷数量，适合代理链路速度较慢、较大文件容易上传超时的场景

- **新增 QQ 官方平台分卷配置**
  - `qq_file_size_limit_mb`：QQ 官方平台单卷大小阈值，默认 `5` MB
  - `qq_max_parts`：单次下载最大分卷数，默认 `5`；预估超过限制时自动提高单卷阈值

### Bug 修复
- **修复 Telegram 上传超时配置未生效**
  - `python-telegram-bot` 的 `ExtBot._request` 实际为请求器二元组，文件上传使用索引 `1` 的普通 API 请求器
  - 现在正确设置该请求器的 `_media_write_timeout`，并同步调整底层 HTTPX 的读写超时
  - 无法识别请求器结构时仅记录警告，不影响插件运行及其他平台

- **修复慢速代理下 Telegram 大文件持续触发 `httpx.WriteTimeout`**
  - 文件超过 Telegram 阈值时自动拆成约 5 MB 的独立分卷并依次发送，减少单次上传耗时

- **修复 QQ 官方平台文件发送受被动回复次数与上传超时影响**
  - QQ 官方平台禁用下载进度，并停止发送封面预览，为最终文件保留被动回复额度并降低内容风控风险
  - 文件上传前将 QQ 官方客户端超时调整为 `120` 秒
  - 保留 QQ 官方平台最大分卷数控制，避免一次下载产生过多被动回复

### 改进
- `/jm`、`/jmc`、`/jmupdate` 统一复用多卷文件发送与清理流程
- 平台阈值选择逻辑集中到兼容工具模块：QQ 与 Telegram 使用各自配置，其他平台保持单文件行为
- 保持现有 AstrBot 消息链及 ZIP/PDF 加密方式，不修改 AstrBot 安装包，避免升级覆盖

### 测试
- 新增 Telegram 请求器二元组、媒体上传超时和平台阈值选择测试
- 新增默认值、自定义值及 `0` 禁用分卷配置测试
- 新增 ZIP 独立分卷、文件不重不漏及各卷统一密码测试
- 单元测试共 `130` 项通过，并通过 JSON 校验、Python 3.12 编译检查和 Git 差异检查
- 使用 114 张图片进行实际分卷验证，成功生成 3 卷且文件无缺失、无重复

---

## **v2.7.6** (2026-06-18)

### Bug 修复
- **修复封面/文件在 NapCat 超时（retcode 1200）下被重复发送、长时间卡顿**
  - NapCat/NTQQ 的 `sendMsg` 超时常伴随 `EventRet {"result": 0}`，表示消息其实已发出，只是客户端没在超时窗口内收到回执。旧逻辑会再压缩重发、纯文字重发、普通重发，导致封面被重复发送多次、流程拖慢约 30s
  - 现在识别到「超时且 result:0」即按已送达处理、直接返回不再重试；真正失败（无 result:0）仍走压缩/纯文字回退

### 文档
- 更新版本号与更新时间

---

## **v2.7.5** (2026-06-18)

### Bug 修复
- **修复多章节本子下载/打包不全**
  - 根因：目录规则为 `Bd/Aid`（不含章节维度），多章节本子的各章都写入 `下载目录/本子ID/`，并以相同页码文件名（00001、00002…）互相覆盖，磁盘最终只剩约一章 → PDF/ZIP 残缺（实测 1114754 共 241 章，第 1、2 章首图均落到同一路径）
  - 目录规则改为 `Bd/Aid/Pindex`，每章存入独立子目录 `…/本子ID/章节序号/`，不再冲突；`decide_album_root_dir` 仍为 `…/本子ID`，打包递归遍历整本
  - 打包按「自然顺序」排序（章节按数字序 1,2,…,10,…,241，而非字典序 1,10,2；页码同理），PDF 与长图均生效

### 文档
- 更新版本号与更新时间

---

## **v2.7.4** (2026-06-18)

### Bug 修复
- **修复多章节本子的下载进度判定**
  - 根因：API 端 `album.page_count` 恒为 0（jmcomic 写死），旧逻辑回退成以「第一章图片数」为分母；多章节本子（可达数百章）进度很早就冲到约 76% 后冻结，而后台仍在下载其余章节
  - 多章节相册改为按「章节」计进度（`after_photo` 逐章累计）；单章节相册与 `/jmc` 仍按「图片」计
  - 完成消息的图片数改用实际下载到的图片计数（此前因 `page_count=0` 恒显示「图片: 0」）
  - 进度轮询步进由 25% 调整为 10%，避免最后一段长时间无反馈；进度文案带单位（如 `66% (2/3 章节)`）

### 文档
- 更新版本号与更新时间

---

## **v2.7.3** (2026-06-18)

### 改进
- **适配 jmcomic 2.7.0** - 依赖下限提升到 `jmcomic>=2.7.0`
  - 经验证为 drop-in 升级：插件使用的同步 API（option 构建、`JmDownloader` 子类、搜索/详情/排行/分类/封面/收藏等）在 2.7.0 上行为不变，无需改动核心逻辑
  - 既有收藏修复（POST `/favorite`、透传 username）在 2.7.0 仍然必要且有效——2.7.0 未修复 `JmApiClient.add_favorite_album` 仍以 GET 请求的问题
- **调试网页转储** - 调试模式下开启 jmcomic 2.7.0 新增的 `FLAG_DUMP_HTML_ON_REGEX_ERROR`，HTML 正则解析失败时自动把网页存到运行工作目录的 `jmcomic_debug/` 下，便于定位解析问题；旧版本无此开关时自动跳过（`hasattr` 守护）

### 文档
- 更新版本号与更新时间

---

## **v2.7.2** (2026-06-18)

### Bug 修复
- **修复 `client_type=html` 时 `/jmfav` 查看收藏失败**
  - HTML 客户端的 `favorite_folder` 需要 `username` 来拼接 `/user/<name>/favorite/albums`，而插件每次新建的 client 不带 `_username`，导致抛“favorite_folder方法需要传username参数”
  - 现在把当前登录用户名透传给 `favorite_folder`（API 端忽略该参数、HTML 端使用），默认 api 模式不受影响

### 新增功能
- **取消收藏** - 新增 `/jmfav del <本子ID>`
  - `add`/`del` 共用幂等逻辑：先用本子详情的 `is_favorite` 判断当前状态，仅当与目标不一致时才切换，避免 toggle 接口被反向操作

### 改进
- **配额模块清理** - 移除无调用方的 `check_quota`/`consume_quota`/`get_remaining`（已由 `reserve`/`refund` 取代）；插件初始化时调用 `cleanup_old_data()`，避免 `quota.db` 随时间无限增长

### 文档
- 帮助信息与收藏提示补充 `/jmfav del` 用法
- 更新版本号与更新时间

---

## **v2.7.1** (2026-06-18)

### Bug 修复
- **修复 `/jmfav add` 收藏失败、且提示为空的问题**
  - 根因：移动端 API 登录得到的会话（`AVS`）只对 API 端有效，对网页（HTML）端无效；旧实现改用 HTML 客户端的 `/ajax/favorite_album`，被服务端判为“未登录”，返回 `{"status":0,"msg":""}`，最终显示为空白的“收藏失败:”
  - 改为复用登录所用的 API 客户端，直接 `POST /favorite`（jmcomic 的 `JmApiClient.add_favorite_album` 以 GET 请求 `/favorite`，等同“列收藏”且响应无 `status` 字段，会触发 `KeyError('status')`，故绕开该方法）
  - 收藏失败时回传服务端真实原因，不再出现空白错误提示

### 改进
- **`/jmfav add` 改为幂等操作**
  - `/favorite` 是“切换（toggle）”接口，对已收藏的本子再次调用会将其取消
  - 现在先用本子详情的 `is_favorite` 字段判断：已收藏直接提示“已在收藏夹中”，仅未收藏时才执行添加，避免“点添加反而取消”

### 文档
- 更新版本号与更新时间

---

## **v2.7.0** (2026-06-17)

### 新增功能
- **本子更新订阅** - 新增 `/jmsub`、`/jmunsub`、`/jmsublist` 命令
  - 订阅后由后台按 `subscribe_check_interval` 间隔检查更新
  - 发现新章节时主动推送通知到对应会话
- **增量下载** - 新增 `/jmupdate <ID>`，仅下载已知章节之后的新增章节
- **搜索类型前缀** - `/jms` 支持 `tag:`、`author:`、`actor:`、`work:` 前缀指定搜索类型
- **排行榜分类** - `/jmrank` 支持附加分类参数，如 `/jmrank week hanman`
- **收藏本子** - 新增 `/jmfav add <ID>`，将本子加入收藏夹
- **长图打包** - 新增 `long_img` 打包格式，纵向拼接页面为长图（过长自动分段并打包为 ZIP）
- **下载进度反馈** - 下载较大本子时按 25% 步进推送进度（`show_download_progress` 可关闭）
- **自定义域名 / 重试次数** - 新增 `client_domain`、`retry_times` 配置项，便于绕过被墙的默认域名

### 改进
- **错误信息分类** - 将 jmcomic 异常（本子不存在 / 重试全部失败 / 部分下载失败）映射为可读提示
- **部分下载检测** - 下载后检查下载器成功/失败统计，残缺下载会明确提示而非按完整处理
- **会话持久化** - 登录后保存真实 cookies，重启后自动恢复登录，无需重新登录

### Bug 修复
- 移除 `DownloadResult.cover_path` 被错误赋值为目录的死代码

### 安全与健壮性修复
- **并发安全**：各操作与下载改用独立的 jmcomic client（`new_jm_client`），避免共享 client 在并发线程下竞态；登录后通过 `update_cookies` 注入会话，且不再缓存已认证 client（`get_client()` 每次新建）
- **凭据保护**：`/jmlogin` 在群聊中被拒绝，避免账号密码泄露到群消息/历史/日志
- **配额原子化**：下载前原子预留配额并在失败时返还，修复并发 TOCTOU 超发；管理员不再被计入配额；数据库异常时 fail-open（放行）并显式告警
- **加密失败关闭**：设置了打包密码但缺少 `pyzipper` 时直接报错，不再静默产出未加密 ZIP
- **错误归类**：搜索/详情/章节/排行/推荐/收藏的网络错误统一向上传播，由 `classify_exception` 区分网络失败与本子不存在，不再被吞为“未找到”或“暂无数据”（章节越界仍提示章节不存在）
- **增量完整性**：增量下载（`/jmupdate`）不再因章节过滤被误报为部分失败
- **订阅开关**：`subscribe_check_interval=0` 时不再启动后台任务
- `pack_format=none` 明确为“本地保存不发送”，结果消息提示已保存到本地（不暴露服务器绝对路径）

### 文档
- 更新 README 命令列表、配置说明与功能特性
- 更新版本号与更新时间

---

## **v2.6.8** (2026-06-14)

### Bug 修复
- **修复首次安装时插件导入失败的问题**
  - 移除 `jmcomic` 的顶层运行期导入，改为在具体功能执行时懒加载
  - 修复依赖尚未安装完成时 `JmOption | None` 注解被运行期求值导致的导入错误
  - 当 `jmcomic` 不可用时，插件可正常加载，下载、搜索和登录功能会按运行期可用性返回提示

### 文档
- 更新 README 的版本、更新时间和 `jmcomic` 依赖版本说明
- 补充首次安装依赖未就绪时的行为说明

---

## **v2.6.7** (2026-06-04)

### Bug 修复
- **修复插件无法安装的问题**
  - 修复 `metadata.yaml` 中 `name` 使用空格和罗马数字导致 AstrBot 无法识别安装的问题
  - 新增 `display_name` 保留 `JM-Cosmos II` 展示名称

---

## **v2.6.6** (2026-01-19)

### Bug 修复
- **修复特殊字符标题导致下载失败的问题**
  - 目录命名规则改为使用本子 ID，避免标题中的特殊字符或过长导致文件系统错误
  - 修复了某些本子（如 ID 1060422）因标题包含大量特殊符号而无法下载的问题

### 新增功能
- **消息发送智能回退机制**
  - 发送超时时自动压缩图片重试（缩小尺寸 + 降低质量）
  - 压缩后仍失败则回退到纯文字发送（保留本子信息，支持撤回）
  - 所有回退方式均支持自动撤回功能
  - 确保消息发送失败不会中断下载流程

### 改进
- **打包失败提示优化**
  - 打包失败时向用户显示具体错误信息
- **临时文件自动清理**
  - 压缩后的临时图片文件会在发送后自动删除

---

## **v2.6.5** (2026-01-09)

### 新增功能
- **封面消息自动撤回** - 新增 `cover_recall_enabled` 配置项
  - 与文件撤回独立控制，可单独开启
  - 仅支持 QQ/NapCat 平台
  - 共用 `auto_recall_delay` 延迟设置

### 改进
- **配置项分组优化** - 重新排序配置项，按功能分组
  - 基础设置、网络设置、下载设置、打包设置
  - 发送设置（含封面撤回、文件撤回、撤回延迟）
  - 权限设置、账号设置、其他设置

---

## **v2.6.4** (2026-01-05)

### 新增功能
- **每日下载次数限制** - 新增 `daily_download_limit` 配置项
  - 基于 SQLite 实现，按用户 QQ 号统计
  - 每日 0 点自动重置
  - 设置为 0 表示不限制
  - **管理员豁免**：`admin_list` 中的用户不受限制

### 新增文件
- `core/quota.py` - 下载配额管理器 `DownloadQuotaManager`

---

## **v2.6.3** (2026-01-05)

### 新增功能
- **文件名显示密码** - 新增 `filename_show_password` 配置项
  - 开启后，打包文件名中会包含密码信息，便于用户识别
  - 默认关闭，保持原有命名方式

### 文档
- README 新增 webp 图片格式说明

---

## **v2.6.2** (2026-01-04)

### Bug 修复
- **Docker 环境代理问题** (Issue #43)
  - 修复在 Docker 环境下即使未配置代理也会尝试连接 `127.0.0.1:7890` 的问题
  - 原因：`jmcomic` 库默认使用系统代理，插件未显式禁用
  - 修复：当 `use_proxy=false` 时，显式设置空代理覆盖 jmcomic 默认行为

### 说明
- 关于依赖缺失问题：请确保 AstrBot Docker 中已安装 `jmcomic` 库
  - 可手动执行：`docker exec -it astrbot pip install jmcomic`

---

## **v2.6.1** (2025-12-31)

### 新增功能
- **搜索翻页功能** - `/jms` 命令支持翻页
  - 命令格式：`/jms <关键词> [页码]`
  - 示例：`/jms 标签名 2` 查看第2页

### 改进
- 优化搜索结果显示
  - 标题长度限制从 30 字符扩展到 50 字符，减少省略
  - 移除冗余的作者行（API 通常返回空值）
  - 添加翻页提示，显示下一页命令

---

## **v2.6.0** (2025-12-30)

### 新增功能
- **自动撤回功能** - 支持发送后自动撤回文件消息（Issue #30）
  - 新增配置项 `auto_recall_enabled` (默认关闭)
  - 新增配置项 `auto_recall_delay` (默认60秒)
  - 仅支持 QQ/NapCat/Lagrange 平台
  - 适用于 `/jm` (整本) 和 `/jmc` (章节) 下载命令

---

## **v2.5.5** (2025-12-28)

### 改进
- 优化打包文件命名格式以降低风控风险
  - 移除敏感前缀，改用时间戳格式
  - `/jm` 命令: `dl_{id}_{timestamp}.zip`
  - `/jmc` 命令: `dl_{id}_ch{n}_{timestamp}.zip`
- 添加文件路径调试日志

---

## **v2.5.4** (2025-12-27)

### Bug 修复
- 修复 `/jmrec` 命令参数重复覆盖问题
  - 同类型参数（如 `new hot`）会被后续参数覆盖的 bug
  - 现在会检测重复参数并提示用户

### 改进
- 改进空结果提示信息，显示查询条件和建议
- 更新 README 添加关于空结果的说明（GitHub NOTE 横幅）

---

## **v2.5.3** (2025-12-27)

### 新增功能
- `/jmrank` 命令支持日排行榜
  - 新增 `day` 类型参数
  - `/jmrank day [页码]` - 查看日排行榜

---

## **v2.5.2** (2025-12-27)

### 架构优化
- 统一常量管理
  - 新增 `core/constants.py` 模块
  - 集中管理分类、排序、时间等常量映射
  - `JMBrowser` 和 `MessageFormatter` 共用常量
- 封面缓存机制优化

---

## **v2.5.1** (2025-12-27)

### Bug 修复
- 修复 `/jmc` 命令无法下载指定章节的问题
  - 原命令只接收一个参数（内部章节ID），用户无法使用
  - 现改为 `/jmc <本子ID> <章节序号>` 格式
  - 例如：`/jmc 1235104 3` 下载本子 1235104 的第 3 章

### 架构优化
- 新增 `browser.get_photo_id_by_index()` 方法，根据本子ID和章节序号获取章节信息

---

## **v2.5.0** (2025-12-27)

### 新增功能
- **推荐浏览功能** - 新增 `/jmrec` 命令，融合推荐与分类浏览
  - `/jmrec [分类] [排序] [时间] [页码]` - 灵活组合浏览条件
  - 支持 9 种分类：`all`/`doujin`/`single`/`short`/`hanman`/`meiman`/`3d`/`cosplay`/`another`
  - 支持 4 种排序：`hot`(热门)/`new`(最新)/`pic`(图多)/`like`(点赞)
  - 支持 4 种时间范围：`day`(今日)/`week`(本周)/`month`(本月)/`all`(全部)
  - 智能参数解析，顺序灵活
  - `/jmrec help` 查看详细帮助

### 架构优化
- 新增 `browser.get_category_albums()` 方法
- 新增 `formatter.format_recommend_results()` 格式化方法
- 在 `JMBrowser` 中添加分类/排序/时间常量映射

---

## **v2.4.1** (2025-12-27)

### Bug 修复
- 修复 `/jmi` 指令不遵循 `send_cover_preview` 配置的问题
- 现在关闭封面预览配置后，`/jmi` 和 `/jm` 都不会发送封面图片

---

## **v2.4.0** (2025-12-27)

### 新增功能
- **封面预览图片** - `/jmi` 和 `/jm` 指令支持发送漫画封面图片
  - `/jmi <ID>` - 现在会同时显示封面图片和详情信息
  - `/jm <ID>` - 当 `send_cover_preview=true` 时发送封面预览
  - 封面缓存到 `{download_dir}/covers/` 目录

### 架构优化
- 新增 `browser.get_album_cover()` 方法用于下载封面

---

## **v2.3.0** (2025-12-27)

### 新增功能
- **收藏夹功能** - 新增 `/jmfav` 命令，查看我的收藏
  - `/jmfav [页码]` - 查看收藏夹（需登录）
  - `/jmfav [页码] [收藏夹ID]` - 查看指定收藏夹
  - 显示收藏夹列表，支持多收藏夹切换

---

## **v2.2.0** (2025-12-27)

### 新增功能
- **登录功能** - 新增账号登录相关命令
  - `/jmlogin <用户名> <密码>` - 登录JM账号
  - `/jmlogout` - 登出账号
  - `/jmstatus` - 查看登录状态
  - 支持在配置中保存凭据，实现自动登录
  - 登录状态持久化保存，重启后自动恢复

### 新增配置项
- `jm_username` - JM账号用户名（可选）
- `jm_password` - JM账号密码（可选）

### 架构优化
- 新增 `core/auth.py` - 认证管理器 `JMAuthManager`
- 支持 cookies 文件持久化存储
- 自动登录和登录状态恢复机制

---

## **v2.1.1** (2025-12-27)

### 架构优化
- **目录结构重构** - 优化 `core/` 模块组织
  - 新增 `core/base/` 子目录，存放基础设施类
  - 将 `config.py` 和 `client.py` 移至 `base/` 目录
  - 改善代码分层，遵循分层架构设计

---

## **v2.1.0** (2025-12-27)

### 新增功能
- **排行榜功能** - 新增 `/jmrank` 命令，支持查看周排行榜和月排行榜
  - `/jmrank week [页码]` - 查看周排行榜
  - `/jmrank month [页码]` - 查看月排行榜
  - 前三名使用🥇🥈🥉特殊显示

### 架构优化
- **模块化重构** - 遵循单一职责原则，拆分核心模块
  - 新增 `client.py` - 客户端管理混入类 `JMClientMixin`
  - 新增 `browser.py` - 浏览查询器 `JMBrowser`（搜索、详情、排行榜）
  - 重构 `downloader.py` - 专注下载功能（下载本子、章节）

---

## **v2.0.1** (2025-12-26)

### Bug 修复
- 修复 `JmAlbumDetail` 属性访问错误，使用 `len(album)` 获取章节数
- 修复 `client_type` 配置错误，正确映射为 `api`/`html`
- 修复 `Comp.File` 文件发送方式
- 修复搜索命令参数处理，支持 AstrBot 参数格式
- 修复搜索 API 调用，使用 `search_site` 和 `iter_id_title_tag`

---

## **v2.0.0** (2025-12-26) - 重大重构

### 架构重构
- 采用模块化设计，代码结构更清晰
- 分离核心逻辑（下载、打包、配置）和工具模块（格式化）
- 使用 `asyncio.to_thread()` 包装同步操作，确保异步兼容性

### 新增功能
- **加密 ZIP 支持** - 使用 `pyzipper` 实现 AES 加密
- **加密 PDF 支持** - 使用 `pymupdf` 实现 PDF 加密
- **章节下载** - 新增 `/jmc` 命令下载单个章节
- **权限控制** - 支持管理员白名单和群组白名单
- **自动清理** - 发送文件后自动删除本地文件
- **封面预览** - 下载前展示漫画详情

### 命令调整
- `/jm <ID>` - 下载完整本子
- `/jmc <ID>` - 下载单个章节（新增）
- `/jms <关键词>` - 搜索漫画
- `/jmi <ID>` - 查看本子详情
- `/jmhelp` - 查看帮助

### 移除功能
- 移除 `/jmimg` 图片预览命令
- 移除 `/jmpdf` PDF 诊断命令
- 移除 `/jmconfig` 配置命令（改用管理面板）
- 移除 `/jmdomain` 域名管理命令
- 移除 `/jmauthor` 作者搜索命令
- 移除 `/jmrecommend` 随机推荐命令

---

## **v1.1.0** (2025-09-20)

### 重大功能更新

- **全新三层下载回退机制** - 显著提高下载成功率
  - 直接客户端下载 → 强制域名配置 → 原始方法回退
  - 智能域名切换，自动处理失效域名
  - 增强的错误处理和重试逻辑

- **作者搜索功能大幅改进**
  - 支持显示作者前 N 部作品列表
  - 智能搜索格式选择
  - 优化搜索结果显示

- **资源管理全面优化**
  - 引入线程池管理
  - 优化内存使用
  - 改进的客户端工厂模式

- **用户体验改善**
  - 更详细的调试信息和日志记录
  - 优化的进度提示和状态反馈

- **代码质量提升**
  - 大幅重构下载逻辑
  - 增强输入验证和异常处理

---

## **v1.0.7** (2025-06-01)

- 新增配置项 `show_cover`，支持控制是否在漫画信息和搜索结果中显示封面图片

---

## **v1.0.6** (2025-05-06)

- 更换文件发送方式，修复文件消息缺少参数问题

---

## **v1.0.5** (2025-05-05)

- 迁移到 AstrBot 官方配置系统，支持在管理面板中配置
- 修复了 API 兼容性问题，使插件适配 AstrBot 最新版本
- 优化了资源管理，现在正确使用 AstrBot 推荐的数据目录
- 改进了错误处理和日志记录
- 添加了线程监控功能

---

## **v1.0.4** (2025-05-05)

- 增加了智能目录识别功能，支持非标准命名的漫画目录
- 改进了图片统计逻辑
- 优化了 `/jmpdf` 命令
- 修复了部分漫画因目录命名问题无法使用 `/jmimg` 命令的问题

---

## **v1.0.3** (2025-05-04)

- 增强了错误处理
- 添加了调试模式
- 添加了网站结构变化的适配
- 修复了 PDF 文件传输失败问题
- 新增图片预览功能和 PDF 文件诊断
- 新增域名测试与自动更新功能

---

## **v1.0.2** (2025-05-03)

- 修复了 PDF 文件传输失败问题
- 新增图片预览功能和 PDF 文件诊断

---

## **v1.0.1** (2025-05-02)

- 增强了错误处理
- 添加了调试模式
- 添加了网站结构变化的适配

---

## **v1.0.0** (2025-05-01)

- 初始版本发布
